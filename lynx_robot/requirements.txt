# Python dependencies for L.Y.N.X. Robot
# Install with: pip3 install -r requirements.txt

# Core dependencies
opencv-python>=4.5.0
numpy>=1.19.0
PyYAML>=5.3.0
Pillow>=8.0.0

# Raspberry Pi GPIO (only needed on Raspberry Pi)
# Uncomment the line below when installing on Raspberry Pi
# RPi.GPIO>=0.7.0
